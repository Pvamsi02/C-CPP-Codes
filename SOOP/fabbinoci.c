#include<stdio.h>
#include<conio.h>
void main() 
{

    int n,a=-1,b=1,c;
    printf("Enter the number:");
    scanf("%d",&n);
    while(n!=0)
    {
        c=a+b
        ;
        printf("%d ",c);
        a=b;
        b=c;
        n--;
    }
    getch(); 
}